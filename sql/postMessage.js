// Task D14
// Post A Message: Front-End (REQUEST) -> API Service (UPDATE) -> RESPONSE
// Finalise and test the implementation of the API service so that a
// specific user can post a new message via the AdaBoard’s front-end.

const postMessage = `
`;

module.exports = { postMessage };