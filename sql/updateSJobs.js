// Task C09
// Create a suitable single SQL instruction that updates Steve Jobs’
// last login details with the system’s current timestamp; your instruction should
// specify the update based on the user’s primary key (in this instance: 1011) rather than the user’s name.

const updateSJobs = `

`;

module.exports = { updateSJobs };